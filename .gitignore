# Build output
/build/
build*/
cmake-build-*/

# Fetched tooling
/.tools/

# JUCE fetch cache (if any local)
_deps/

# CI staging
/dist/

# Editor / OS cruft
.DS_Store
.vscode/
.idea/
*.user
*.swp

# Python bytecode — scripts/check-docs.py leaves this behind when imported
# (e.g. by its own --self-test run from another directory).
__pycache__/
*.py[cod]
